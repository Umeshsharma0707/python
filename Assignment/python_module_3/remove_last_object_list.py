#•que : How will you remove last object from a list?
"""
we can simply remove the last item from the list using 
the pre-built function pop().
if we will not give the index in the pop(index) function it 
removes the last item of the list
"""
#removing the last item of the list

list = [1,2,3,4,5,6]

list.pop() # it removes the last item of the list
print(list)